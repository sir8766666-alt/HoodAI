{
  "engines": {
      "vscode": "^1.90.0"
      },
      "main": "./out/extension.js",
      "activationEvents": ["*"]
  "name":"hoodai-browser-extension",
  "displayName": "HoodAI",
  "description": "Shows developer-sponsored ads while AI tools are generating responses.",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc",
    "watch": "tsc --watch",
    "zip": "zip -r hoodai-browser.zip . -x node_modules/* -x .git/*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.30",
    "@types/node": "^24.13.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vscode": "^1.1.37"
  }
}
